program testPL0;

const year      = 1998, em = 1;
      foo       = -10;
	  achar     = 'a';
	  ninenum   = '9';

var ch                  : char;
    bdate               : integer;
    i                   : integer;
    line                : array[1..81] of char;
    sfile               : string;
    yesno               : boolean;
    firstname, surname  : string;

procedure times;
    begin
        i := 1;
        writeln('i= ', i);
        i := i * 10;
        writeln('i times 10= ', i);
    end

procedure myname;
    begin
        writeln('Please enter your first name: ');
        readln(firstname);
   
        writeln('Please enter your surname: ');
        readln(surname);

        writeln('Please enter your birthdate: ');
        readln(bdate);
   
        writeln(message, ' ', firstname, ' ', surname);
        writeln('I born in', bdate, ',', year);
    end

procedure conditions;
    begin
        if i+a<>0 then
            writeln('Not equal working well');
        if 0=0
            writeln('Equal working well');
        if 2>=1
            writeln('Bigger or equal than working well');
		if bdate > 15
            writeln('My birthdate is after the 15th of the month');
        else
            writeln('My birtdate is before the 15th of the month');
    end

function times ( i, a : integer; x : string ): integer;
    var it : integer;
    begin
        it := 50;
        it := it[i+1] + 1;
        writeln('i= ', i);
        it := it - i;
        writeln('i= ', i);
        it := it * i;
        writeln('i= ', i);
        it := it / i;
        writeln('i= ', i);
        times := it;
    end;

procedure repeatthis;
    var a: integer;
    begin
        a := 10;
        repeat
            writeln('value of a: ', a);
            a := a + 1;
        until a = 20;
    end

procedure forloop;
    var a: integer;
    begin
        for a := 10  to 20 do
   
        begin
            writeln('value of a: ', a);
        end;
    end

begin
    myname(a+20);

    mathfunc;

    conditions;

    writeln(times(2));

    repeatthis;
    forloop;
end.