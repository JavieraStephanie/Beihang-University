1、实现TCP全连接扫描：相当于写一个Socket客户端，协议格式为TCP。
2、对指定目标主机实现端口扫描：即可以设置Socket客户端要连接的服务器IP，端口则设置在一定范围内进行循环扫描（可以；利用多线程提高扫描速度）。
3、判断端口是否开放：判断Socket连接是否成功。
4、为了观察到端口开放现象，可以通过设置防火墙规则打开端口；也可以在关闭防火墙后，在被扫描的主机端开启FTP服务器、Web服务器等。