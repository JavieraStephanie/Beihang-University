


// Instantiate the module
dm instance_name (
    .clk(clk), 
    .reset(reset), 
    .MemWrite(MemWrite), 
    .A(A), 
    .WData(WData), 
    .PC(PC), 
    .addr(addr), 
    .RData(RData)
    );


